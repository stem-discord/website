const userInfoPublicSchema = require(`${__dirname}/schemas/userInfoPublic.js`);
const thankedLogSchema = require(`${__dirname}/schemas/thankedLog.js`);
const aggregateUserInfoSchema = require(`${__dirname}/schemas/aggregateUserInfo.js`);

module.exports = {
  userInfoPublicSchema,
  thankedLogSchema,
  aggregateUserInfoSchema,
};